Personal Test

<% #form_for @user, url: wizard_path, method: :put do|f| %>

<%= form_for(@user, url: wizard_path) do |f| %>

  <% if f.object.errors.any? %>
  <div class="error_messages">
    <% f.object.errors.full_messages.each do |error| %>
      <p><%= error %></p>
    <% end %>
  </div>
  <% end %>

  <div class="form-group">
    <%= f.email_field :email, :placeholder => "Email", :class => 'form-control' %>
  </div>

  <div class="form-group">
    <%= f.text_field :first_name, autofocus: true, :placeholder => "First Name", :class => 'form-control' %>
  </div>

  <div class="form-group">
    <%= f.text_field :last_name, autofocus: true, :placeholder => "Last Name", :class => 'form-control' %>
  </div>

  <div class="form-group">
    <%= f.text_field :phone_number, :placeholder => "Phone Number", :class => 'form-control' %>
  </div>

  <div class="form-group">
    <%= f.text_area :description, rows: 5, cols: 25,  :placeholder => "About Me", :class => 'form-control' %>
  </div>

  <div class="actions">
     <%= f.submit 'Next', :class => 'btn btn-primary' %>
  </div>

<% end %>
